---
// No se necesita JS
---
<section class="w-full py-20" id="contacto">
  <div class="bg-background-card rounded-xl p-8 md:p-12"> 
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
      
      <div class="flex flex-col gap-6">
        <h2 class="text-light text-3xl md:text-4xl font-black leading-tight tracking-tighter">Contáctenos</h2>
        <p class="text-text-muted text-base font-normal leading-normal">
          ¿Listo para optimizar su operación? Complete el formulario para recibir una cotización personalizada o contáctenos directamente.
        </p>
        <div class="flex flex-col gap-4">
          <div class="flex items-center gap-4">
            <span class="material-symbols-outlined text-primary">location_on</span>
            <p class="text-light">Av. Industrial 123, Parque Minero, Calama, Chile</p>
          </div>
          <div class="flex items-center gap-4">
            <span class="material-symbols-outlined text-primary">phone</span>
            <p class="text-light">+56 9 1234 5678</p>
          </div>
          <div class="flex items-center gap-4">
            <span class="material-symbols-outlined text-primary">mail</span>
            <p class="text-light">ventas@rumbomotriz.cl</p>
          </div>
        </div>
      </div>

      <form class="flex flex-col gap-4">
        
        <label for="name" class="sr-only">Nombre Completo</label>
        <input 
          id="name"
          name="name"
          class="w-full h-12 px-4 bg-background-main border border-theme rounded-lg text-light focus:ring-primary focus:border-primary" 
          placeholder="Nombre Completo" 
          type="text"
        />

        <label for="company" class="sr-only">Empresa</label>
        <input 
          id="company"
          name="company"
          class="w-full h-12 px-4 bg-background-main border border-theme rounded-lg text-light focus:ring-primary focus:border-primary" 
          placeholder="Empresa" 
          type="text"
        />

        <label for="email" class="sr-only">Email</label>
        <input 
          id="email"
          name="email"
          class="w-full h-12 px-4 bg-background-main border border-theme rounded-lg text-light focus:ring-primary focus:border-primary" 
          placeholder="Email" 
          type="email"
        />

        <label for="phone" class="sr-only">Teléfono</label>
        <input 
          id="phone"
          name="phone"
          class="w-full h-12 px-4 bg-background-main border border-theme rounded-lg text-light focus:ring-primary focus:border-primary" 
          placeholder="Teléfono" 
          type="tel"
        />

        <label for="message" class="sr-only">Mensaje</label>
        <textarea 
          id="message"
          name="message"
          class="w-full p-4 bg-background-main border border-theme rounded-lg text-light focus:ring-primary focus:border-primary" 
          placeholder="Mensaje" 
          rows="4"
        ></textarea>
        
        <button class="flex min-w-[84px] cursor-pointer items-center justify-center overflow-hidden rounded-lg h-12 px-6 bg-primary text-white text-base font-bold leading-normal tracking-[0.015em] hover:bg-primary/90 transition-colors" type="submit">
          <span class="truncate">Enviar Consulta</span>
        </button>
      </form>
    </div>
  </div>
</section>