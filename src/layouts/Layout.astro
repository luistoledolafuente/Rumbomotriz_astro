---
// 1. Importamos los estilos globales que ya creamos
import '../styles/global.css';
import "../styles/custom.css";


// 2. Definimos las "props" que este layout aceptará.
// En este caso, un "title" para la pestaña del navegador.
export interface Props {
  title: string;
}
const { title } = Astro.props;
---
<!DOCTYPE html>
<!-- Tomamos esto de tu HTML original -->
<html class="dark" lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  
  <!-- El título de la página (viene de las props) -->
  <title>{title}</title>

  <!-- Fuentes de Google (copiadas de tu HTML) -->
  <link href="https://fonts.googleapis.com" rel="preconnect" />
  <link href="https://fonts.gstatic.com" rel="preconnect" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700;900&display=swap" rel="stylesheet" />
  
  <!-- Iconos de Material Symbols (copiados de tu HTML) -->
  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet" />
</head>

<!-- Clases de Tailwind aplicadas al body (copiadas de tu HTML) -->
<body class="bg-background-light dark:bg-background-dark text-text-light font-display">
  
  <!-- <slot /> es el marcador de Astro. -->
  <!-- Aquí es donde se inyectará el contenido de tus páginas (ej. index.astro) -->
  <slot />

</body>
</html>